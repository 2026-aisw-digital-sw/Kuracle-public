# Action Agent

Turns a knowledge result into an executable administrative checklist, draft, or
form-preparation plan. It is disabled by default until the document/action bridge
is promoted.

Runtime reference: `src/hobit_ax_agentos/agents/action.py`.

# Coordinator Agent

Builds the AgentOS task graph and records why each agent was selected or skipped.

Own this folder when changing request classification, routing reasons, selected
agent sets, or graph planning rules. Keep domain answering inside Knowledge Agent.

Runtime reference: `src/hobit_ax_agentos/agents/coordinator.py`.

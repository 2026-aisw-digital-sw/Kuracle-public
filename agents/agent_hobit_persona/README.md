# Persona Worker

Builds deterministic session context from the current message and conversation
history. The output improves routing, suggested questions, and future proactive
recommendations.

Runtime reference: `src/hobit_ax_agentos/agents/persona.py`.

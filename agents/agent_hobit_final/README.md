# Final Response Agent

Composes the user-facing response from knowledge, optional action plan, and optional
escalation result. It also shapes the delivery payload that goes to outbox.

Runtime reference: `src/hobit_ax_agentos/agents/final_response.py`.

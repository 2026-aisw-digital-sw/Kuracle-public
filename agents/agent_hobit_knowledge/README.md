# Knowledge Agent

Runs Hobit regulation reasoning through the `regulation_rag` adapter and returns
a structured answer plus confidence, citation, action, and review signals.

Runtime reference: `src/hobit_ax_agentos/agents/knowledge.py`.
